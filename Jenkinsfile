pipeline {
    agent any
    tools {
        nodejs "node18"
    }
    stages {
        stage('Build React App') {
            steps {
                sh "ls -al"
                sh "cd ./device/frontend"
            }
        }
    }
}